description: >
  Connect to VPN, so we can control the live cluster

steps:
  - run:
      name: Open ssh tunnel
      command: |
        echo $SSH_KEY_PROD | base64 --decode > ssh_key
        ssh -i ssh_key -L 10666:10.10.27.102:443 -Nf ubuntu@vpn.trading.io
