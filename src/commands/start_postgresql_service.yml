description: >
  Starts Postgre SQL service
steps:
  - run:
      name: "Start Postgre SQL service"
      command: |
        service postgresql start
        su postgres
        psql
        postgres=# CREATE USER test PASSWORD test;
        postgres=# ALTER USER test WITH SUPERUSER;
        postgres=# \q
