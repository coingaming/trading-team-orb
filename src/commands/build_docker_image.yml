description: >
  Build Docker Image
parameters:
  full_version:
    type: string
steps:
  - run:
      name: "Running docker build"
      command: |
        docker build -f CircleDockerfile . -t $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$DOCKER_PROJECT_NAME:<<parameters.full_version>>
